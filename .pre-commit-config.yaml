# repos:
#   - repo: https://github.com/gitleaks/gitleaks
#     rev: v8.24.2   # เวอร์ชันล่าสุดตอนนี้
#     hooks:
#       - id: gitleaks
#         args: ["--config-path=.gitleaks.toml"]

repos:
  - repo: local
    hooks:
      - id: gitleaks-staged
        name: gitleaks (staged only)
        entry: gitleaks git --staged --verbose
        language: system
        pass_filenames: false
        verbose: true