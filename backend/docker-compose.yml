services:
  backend:
    image: backend:${BACKEND_VERSION}
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - .env.docker
    restart: unless-stopped