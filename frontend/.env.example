VITE_BASE_API=/api

# Idle timeout before showing lock screen (in minutes)
# Default: 15 minutes
VITE_IDLE_TIMEOUT_MINUTES=15

# Lock screen countdown before auto-logout (in minutes)
# Default: 10 minutes
VITE_LOCK_SCREEN_TIMEOUT_MINUTES=10
