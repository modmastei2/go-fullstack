apiVersion: v1
kind: Secret
metadata:
  name: minio-secret
  namespace: go-fullstack
type: Opaque
stringData:
  # เปลี่ยน credentials เหล่านี้ใน production
  root-user: EXAMPLE_DEV_MINIO_ROOT_USER
  root-password: EXAMPLE_DEV_MINIO_ROOT_PASSWORD
